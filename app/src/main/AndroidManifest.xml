<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackaprende.dogedex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dogedex">
        <activity
            android:name=".ui.user.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.dog.dogDetail.DogDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.dog.dogList.DogListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>